## Netlify custom headers for security stuff
## ref: https://www.netlify.com/docs/headers-and-basic-auth/
## ref: https://www.josephearl.co.uk/post/static-sites-netlify-security/

/*
  # Prevent the website from being embedded as iFrame
  X-Frame-Options: DENY
  X-XSS-Protection: 1; mode=block

  # Prevent browsers from incorrectly detecting non-scripts as scripts
  X-Content-Type-Options: nosniff

  # Don't load any resource type not explicitly enabled
  # Disable plugins like Flash or Silverlight
  # Load images, stylesheets and fonts from self
  Content-Security-Policy: default-src 'none'; object-src 'none'; img-src 'self'; style-src 'self';

  # To enable the following to disable script-src, style-src and font-src to self

/assets/aura/neutrogena_widgets/*
  # Prevent the website from being embedded as iFrame
  X-Frame-Options: ALLOW
